apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: OCIRepository
metadata:
  name: {{ .Values.clusterID }}-aws-load-balancer-controller
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aws-load-balancer-controller-bundle.labels" . | nindent 4 }}
spec:
  url: {{ .Values.ociRepositoryUrl }}
  interval: 1h
  ref:
    tag: {{ .Chart.Version }}
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: {{ .Values.clusterID }}-aws-load-balancer-controller
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aws-load-balancer-controller-bundle.labels" . | nindent 4 }}
spec:
  interval: 5m
  install:
    createNamespace: true
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: -1
  kubeConfig:
    secretRef:
      name: {{ .Values.clusterID }}-kubeconfig
  chartRef:
    kind: OCIRepository
    name: {{ .Values.clusterID }}-aws-load-balancer-controller
    namespace: {{ .Release.Namespace }}
  releaseName: aws-load-balancer-controller
  targetNamespace: aws-load-balancer-controller
  storageNamespace: aws-load-balancer-controller
  valuesFrom:
    - kind: ConfigMap
      name: {{ .Values.clusterID }}-aws-load-balancer-controller-config
      valuesKey: values
      optional: false
