version: 2.1
orbs:
  architect: giantswarm/architect@8.3.0

job_filters: &job_filters
  filters:
    tags:
      only: /^v[0-9]+\.[0-9]+\.[0-9]+$/
    branches:
      ignore:
        - main

workflows:
  package-and-push-chart-on-tag:
    jobs:
      - architect/push-to-app-catalog:
          context: architect
          executor: app-build-suite
          name: push-aws-load-balancer-controller-to-giantswarm-catalog
          app_catalog: giantswarm-catalog
          app_catalog_test: giantswarm-test-catalog
          chart: aws-load-balancer-controller
          ct_config: ".circleci/ct-config.yaml"
          explicit_allow_chart_name_mismatch: true
          <<: *job_filters

      - architect/push-to-app-catalog:
          context: architect
          executor: app-build-suite
          name: push-aws-lb-controller-bundle-to-giantswarm-catalog
          app_catalog: giantswarm-catalog
          app_catalog_test: giantswarm-test-catalog
          chart: aws-lb-controller-bundle
          ct_config: ".circleci/ct-config.yaml"
          explicit_allow_chart_name_mismatch: true
          <<: *job_filters

      - architect/push-to-app-catalog:
          context: architect
          executor: app-build-suite
          name: push-aws-lb-controller-bundle-to-control-plane-catalog
          app_catalog: control-plane-catalog
          app_catalog_test: control-plane-test-catalog
          chart: aws-lb-controller-bundle
          ct_config: ".circleci/ct-config.yaml"
          explicit_allow_chart_name_mismatch: true
          <<: *job_filters

